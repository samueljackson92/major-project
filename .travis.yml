language: python
python:
  - "2.7"

before_install:
  - pip install six
  - pip install numpy
# command to install dependencies
install:
  # - "pip install ."
  - "pip install -r requirements.txt"
# command to run tests
script: nosetests
